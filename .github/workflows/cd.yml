name: CD

on:
  push:
    branches: [ "main" ]
    tags:
      - "v*"
      - "release-*"

permissions:
  contents: read
  packages: write
  id-token: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    name: Build & Push Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build & Push
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image digest
        run: echo "Pushed digest ${{ steps.build.outputs.digest }}"

  # Optional deployment via SSH to a server running Docker or Docker Compose.
  # Fill in your secrets and uncomment this job if you want auto-deploy.
  # deploy:
  #   name: Deploy to Server
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   if: github.ref == 'refs/heads/main'  # deploy only from main (adjust as needed)
  #   steps:
  #     - name: Pull & restart on remote host
  #       uses: appleboy/ssh-action@v1.2.0
  #       with:
  #         host: ${{ secrets.DEPLOY_HOST }}
  #         username: ${{ secrets.DEPLOY_USER }}
  #         key: ${{ secrets.DEPLOY_SSH_KEY }}
  #         port: ${{ secrets.DEPLOY_PORT || 22 }}
  #         script: |
  #           set -euo pipefail
  #           IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
  #           docker pull "$IMAGE"
  #           # If using docker compose:
  #           # cd /opt/your-app
  #           # docker compose pull
  #           # docker compose up -d --remove-orphans
  #           # Or a simple single-container restart:
  #           docker rm -f your-app || true
  #           docker run -d --name your-app -p 8000:8000 "$IMAGE"
